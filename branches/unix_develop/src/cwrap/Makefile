include ../../Makefile.config

PROJ_ROOT	:= $(CURDIR)/../..
obj_dir		:= $(ROOT_OBJ_DIR)/cwrap
bin_dir		:= $(ROOT_BIN_DIR)
binary		:= cwrap
CPP_INCLPATHS	= $(PROJ_ROOT)

include ../../Makefile.include
